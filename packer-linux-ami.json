{
  "builders": [
    {
      "type": "amazon-chroot",
      "region": "us-west-2",
      "source_ami_filter": {
        "filters": {
          "virtualization-type": "hvm",
          "name": "amzn2-ami-hvm-*-x86_64-gp2",
          "root-device-type": "ebs"
        },
        "owners": ["amazon"],
        "most_recent": true
      },
      "ami_name": "linux-docker-ami-{{timestamp}}",
      "mount_options": "bind",
      "mount_path": "{{ env.RUNNER_TEMP }}/packer-amazon-chroot",
      "chroot_mounts": [
        ["proc", "proc", "{{ env.RUNNER_TEMP }}/packer-amazon-chroot/proc"],
        ["sysfs", "sysfs", "{{ env.RUNNER_TEMP }}/packer-amazon-chroot/sys"],
        ["bind", "/dev", "{{ env.RUNNER_TEMP }}/packer-amazon-chroot/dev"],
        ["bind", "/dev/pts", "{{ env.RUNNER_TEMP }}/packer-amazon-chroot/dev/pts"],
        ["bind", "/proc/sys/fs/binfmt_misc", "{{ env.RUNNER_TEMP }}/packer-amazon-chroot/proc/sys/fs/binfmt_misc"]
      ],
      "command_wrapper": "sudo {{.Command}}",
      "copy_files": [
        "/etc/resolv.conf"
      ],
      "device_path": "/dev/sdb",
      "root_volume_size": 20,
      "force_deregister": true
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "./mount_chroot.sh"
    },
    {
      "type": "shell",
      "script": "./install_docker.sh"
    }
  ]
}
